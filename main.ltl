// ltl-stats-basic/main.ltl
//
// Mean / variance / quantile / moments for streaming data.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-stats-basic v0.1.0")
    io::println("Mean / variance / quantile / moments for streaming data.")
    0
}
